class Task {
  int id;
  String name;
  String description;
  int memoryLimit;
  int timeLimit;
  String authorName;

  Task(this.id, this.name, this.description, this.memoryLimit, this.timeLimit,
      this.authorName);
}
