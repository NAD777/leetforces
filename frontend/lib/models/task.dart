class Task {
  int id;
  String name;
  String description;
  int memoryLimit;
  int timeLimit;
  int testsCount;

  Task(this.id, this.name, this.description, this.memoryLimit, this.timeLimit,
      this.testsCount);
}
